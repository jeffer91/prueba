# Estado

La base inicial fue subida al repositorio.

Queda pendiente completar algunos archivos desde VS Code porque el conector bloqueo ciertas rutas tecnicas durante la carga.

Pasos recomendados en la PC:

npm install
npm run check
npm start

git add .
git commit -m "feat: completar app"
git push origin main
